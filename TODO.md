
# Arrumar:
- Fazer locust testar apenas uma api de cada vez para remover a poluição de falhas
- `graphql-api    | ERROR:    Exception in ASGI application` depois de `docker compose --profile db --profile graphql --profile loadtest up --build`

